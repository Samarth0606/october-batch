const express = require('express');
const app = express(); // entire application (instance of your application)


app.get()
app.post()
app.put()
app.patch()
app.delete()

const PORT = 8080;
app.listen(PORT,()=>{
    console.log(`server connected at port: ${PORT}`); 
})






